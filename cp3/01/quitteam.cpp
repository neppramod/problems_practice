#include <bits/stdc++.h>

using namespace std;

int main()
{
    int N;
    scanf("N: %d", &N);
    int x[20], y[20];

    for (int i = 0; i < N; i++) {
        scanf("X, Y: %d, %d", &x[i], &y[i]);
    }






    return 0;
}
